export default class User {
    constructor(name, username, password, _id) {
        this.name = name;
        this.username = username;
        this.password = password;
        this._id = _id;
    }
}
